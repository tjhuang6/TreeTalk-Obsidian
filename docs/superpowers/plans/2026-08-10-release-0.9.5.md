# TreeTalk 0.9.5 Release Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Publish the locally verified TreeTalk 0.9.5 source, refreshed bilingual README, and installable plugin archive to `safasffa111/TreeTalk-Obsidian`.

**Architecture:** Treat the verified local source directory as the release payload and the cloned GitHub repository as the only Git working copy. Synchronize project files without copying local dependencies or backups, then validate the resulting checkout before pushing, merging, tagging, and attaching the already verified installer archive.

**Tech Stack:** TypeScript, Obsidian Plugin API, npm, Vitest, ESLint, esbuild, Git, GitHub CLI.

## Global Constraints

- Release version is exactly `0.9.5`.
- Target repository is `safasffa111/TreeTalk-Obsidian`; target branch is `main`.
- Source payload is `E:\继续\TreeTalk-archive-20260809\TreeTalk-Obsidian-0.9.4-source`.
- Do not publish `node_modules`, local backup archives, temporary files, credentials, or unrelated workspace files.
- Preserve the bilingual README structure with Chinese first and English second.
- The release installer must contain `main.js`, `manifest.json`, and `styles.css` and retain SHA-256 `382020F60884BEC38A93CA29324B8BA387F473E7A83D031CCEEF4A8796465EE4`.
- Do not claim unsupported providers, mobile support, offline models, or whole-vault semantic search.

---

### Task 1: Synchronize the verified 0.9.5 project

**Files:**
- Modify: repository project files that differ from the verified source
- Preserve: `.git/`
- Exclude: `node_modules/`, local archives and temporary artifacts

**Interfaces:**
- Consumes: verified source tree and cloned `agent/release-0.9.5` branch
- Produces: a Git checkout whose project content matches the verified source tree

- [ ] **Step 1: Preview the synchronization scope**

Run a dry-run copy preview that excludes `.git`, `node_modules`, and internal planning documents:

```powershell
robocopy "E:\继续\TreeTalk-archive-20260809\TreeTalk-Obsidian-0.9.4-source" "E:\维护\TreeTalk-Obsidian-release-0.9.5" /E /L /XD node_modules .git "E:\继续\TreeTalk-archive-20260809\TreeTalk-Obsidian-0.9.4-source\docs\superpowers" /R:2 /W:1
```

Expected: only TreeTalk project files appear; no backup archive, dependency directory, credential file, or unrelated workspace file is listed.

- [ ] **Step 2: Synchronize the reviewed files**

```powershell
robocopy "E:\继续\TreeTalk-archive-20260809\TreeTalk-Obsidian-0.9.4-source" "E:\维护\TreeTalk-Obsidian-release-0.9.5" /E /XD node_modules .git "E:\继续\TreeTalk-archive-20260809\TreeTalk-Obsidian-0.9.4-source\docs\superpowers" /R:2 /W:1
```

Expected: source, tests, regression tests, configuration, version metadata, `main.js`, `styles.css`, and public documentation match the verified source while `.git` and the release design/plan remain intact. A read-only file-set comparison has already confirmed that no tracked repository file is absent from the verified source, so no deletion step is required.

- [ ] **Step 3: Verify version metadata and working-tree scope**

```powershell
git status -sb
rg -n '"version"\s*:\s*"0\.9\.5"|"0\.9\.5"' package.json package-lock.json manifest.json versions.json
```

Expected: all intended project changes are visible and all four release metadata files resolve to 0.9.5.

### Task 2: Refresh the bilingual README

**Files:**
- Modify: `README.md`

**Interfaces:**
- Consumes: the current 0.9.5 feature set and approved README design
- Produces: accurate Chinese-first and English-second release documentation

- [ ] **Step 1: Update the opening positioning and version**

Replace the stale current-version statements with `0.9.5` and describe TreeTalk as a traceable, branching Obsidian AI research workflow that deposits results into plain Markdown.

- [ ] **Step 2: Add a concise 0.9.5 highlights section in both languages**

The section must list exactly these user-visible stability improvements:

```text
- Targeted streaming UI updates
- Adaptive Markdown rendering cadence
- Incremental history indexing
- Ordered startup loading with at most four concurrent conversation reads
```

- [ ] **Step 3: Preserve detailed usage and privacy documentation**

Confirm the existing tree interaction, selection context, relationship graph, Markdown deposit, SecretStorage, data location, and network disclosure sections remain present in Chinese and English.

- [ ] **Step 4: Check README consistency**

```powershell
rg -n "0\.9\.0|0\.9\.2|Current version|当前为|0\.9\.5|受控并发|controlled concurrency" README.md
```

Expected: no stale current-version claim remains; 0.9.5 and the startup-loading improvement appear in both language sections.

### Task 3: Validate the release checkout

**Files:**
- Verify: all synchronized source and test files
- Verify: `main.js`, `manifest.json`, `styles.css`

**Interfaces:**
- Consumes: synchronized checkout and refreshed README
- Produces: evidence that the exact GitHub candidate passes the complete project quality gate

- [ ] **Step 1: Install locked dependencies**

```powershell
npm ci
```

Expected: dependency installation exits with code 0 using `package-lock.json`.

- [ ] **Step 2: Run the complete quality gate**

```powershell
npm run check
```

Expected: 93 Vitest files and 515 tests pass; typecheck, lint, build pass; 328 Node regression tests pass.

- [ ] **Step 3: Validate the installer archive**

```powershell
$zip="C:\Users\m2645\Documents\Codex\2026-08-10\ni\outputs\TreeTalk-Obsidian-0.9.5-backup-20260810\TreeTalk-Obsidian-0.9.5-installer.zip"
tar -tf $zip
Get-FileHash -Algorithm SHA256 -LiteralPath $zip
```

Expected: the archive contains only `main.js`, `manifest.json`, and `styles.css`; its SHA-256 matches the global constraint.

- [ ] **Step 4: Review the final diff**

```powershell
git diff --check
git status --short
git diff --stat
git diff -- README.md package.json manifest.json versions.json
```

Expected: no whitespace error, credential, dependency directory, backup archive, or unrelated change is present.

### Task 4: Publish the branch and pull request

**Files:**
- Commit: all reviewed 0.9.5 project and documentation changes

**Interfaces:**
- Consumes: validated release candidate
- Produces: a GitHub pull request from `agent/release-0.9.5` to `main`

- [ ] **Step 1: Stage only the reviewed release files**

```powershell
git add -A
git status --short
```

Expected: staging contains only the reviewed release scope.

- [ ] **Step 2: Commit the release candidate**

```powershell
git commit -m "release: TreeTalk 0.9.5"
```

Expected: a new commit is created on `agent/release-0.9.5`.

- [ ] **Step 3: Push the branch**

```powershell
git push -u origin agent/release-0.9.5
```

Expected: the remote branch is created with upstream tracking.

- [ ] **Step 4: Create the pull request**

Create a PR targeting `main` whose title is `Release TreeTalk 0.9.5`. Its body must summarize source changes, streaming and startup performance improvements, README refresh, user impact, and the complete quality-gate result.

### Task 5: Merge and create the 0.9.5 GitHub Release

**Files:**
- Attach: `TreeTalk-Obsidian-0.9.5-installer.zip`
- Create temporarily: `E:\维护\release-notes-0.9.5.md`

**Interfaces:**
- Consumes: approved and mergeable release PR
- Produces: updated `main`, Git tag `0.9.5`, and downloadable GitHub Release

- [ ] **Step 1: Confirm PR checks and mergeability**

```powershell
gh pr checks --repo safasffa111/TreeTalk-Obsidian
gh pr view --repo safasffa111/TreeTalk-Obsidian --json state,mergeable,headRefName,baseRefName,statusCheckRollup
```

Expected: the PR targets `main`, reports mergeable, and has no failing required checks.

- [ ] **Step 2: Merge the release PR**

```powershell
gh pr merge --repo safasffa111/TreeTalk-Obsidian --squash --delete-branch
```

Expected: `main` contains the 0.9.5 release candidate and the remote feature branch is removed.

- [ ] **Step 3: Create the GitHub Release**

Create tag `0.9.5` from the updated default branch and attach the verified installer:

```powershell
gh release create 0.9.5 "C:\Users\m2645\Documents\Codex\2026-08-10\ni\outputs\TreeTalk-Obsidian-0.9.5-backup-20260810\TreeTalk-Obsidian-0.9.5-installer.zip" --repo safasffa111/TreeTalk-Obsidian --target main --title "TreeTalk 0.9.5" --notes-file "E:\维护\release-notes-0.9.5.md"
```

The release notes must cover the controlled startup concurrency, streaming rendering improvements, incremental history indexing, unchanged local data compatibility, and the complete validation result.

- [ ] **Step 4: Verify the published result**

```powershell
gh repo view safasffa111/TreeTalk-Obsidian --json defaultBranchRef,url
gh release view 0.9.5 --repo safasffa111/TreeTalk-Obsidian --json url,tagName,name,assets
```

Expected: `main` points to the merged update and release `0.9.5` exposes the installer asset.
